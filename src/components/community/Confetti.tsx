/**
 * Confetti — Backwards-compatible re-export
 *
 * The canonical implementation has moved to src/components/effects/Confetti.tsx.
 * This file re-exports it so existing imports continue to work.
 */

export { Confetti, default } from '../effects/Confetti';
