node_modules
.DS_Store
.expo/
.env
.mcp.json

# Native directories (regenerated by EAS prebuild)
ios/
android/

# Build output
dist/

# Reference images (stored locally, not needed for builds)
Images/

# Build artifacts / temp files
tsc_errors.txt
tsc_output.txt
filtered_tsc.txt
supabase/.temp/
expo_web.log

# Python
**/__pycache__/
*.pyc
venv/
.pytest_cache/
.env.python

# Post-match flow test state (contains test credentials)
pm-test-state.json

# Supabase query helper (contains service role key)
scripts/supabase-query.sh

# Iconscout raw assets (large zips and source directories)
assets/iconscout/*.zip
assets/iconscout/mental-health-143/
assets/iconscout/sport-215/
assets/iconscout/stay-at-home-14/
.claude/settings.local.json
